<div class="container">
  <h1><%= @list.name %></h1>
  <%# <ul> %>
    <% @favorites.each do |fave| %>
      <%= render 'shared/restaurant_card', restaurant: fave.restaurant %>
    <% end %>
  <%# </ul> %>
  <div class="delete-link">
    <%= link_to "Delete list", list_path(@list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "link-text" %>
  </div>
  <div class="back-link">
    <%= link_to "Back to selections", restaurants_path, class: "link-text" %>
  </div>
</div>
